apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  ports:
  - protocol: TCP
    port: 443
    targetPort: 80
  externalIPs:
  - 192.168.1.94
  type: LoadBalancer
